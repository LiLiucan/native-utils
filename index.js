/* tslint:disable */
/* eslint-disable */
/* prettier-ignore */

/* auto-generated by NAPI-RS */

const { readFileSync } = require('fs')

const { platform, arch } = process;

let nativeBinding = null;
let loadError = null;

function isMusl() {
  // For Node 10
  if (!process.report || typeof process.report.getReport !== "function") {
    try {
      const lddPath = require("child_process")
        .execSync("which ldd")
        .toString()
        .trim();
      return readFileSync(lddPath, "utf8").includes("musl");
    } catch (e) {
      return true;
    }
  } else {
    const { glibcVersionRuntime } = process.report.getReport().header;
    return !glibcVersionRuntime;
  }
}

switch (platform) {
  case "win32":
    switch (arch) {
      case "x64":
        try {
          nativeBinding = require("./native-util.win32-x64-msvc.node");
        } catch (e) {
          loadError = e;
        }
        break;
      case "ia32":
        try {
          nativeBinding = require("./native-util.win32-ia32-msvc.node");
        } catch (e) {
          loadError = e;
        }
        break;
      case "arm64":
        try {
          nativeBinding = require("./native-util.win32-arm64-msvc.node");
        } catch (e) {
          loadError = e;
        }
        break;
      default:
        throw new Error(`Unsupported architecture on Windows: ${arch}`);
    }
    break;
  case "darwin":
    switch (arch) {
      case "x64":
        try {
          nativeBinding = require("./native-util.darwin-x64.node");
        } catch (e) {
          loadError = e;
        }
        break;
      case "arm64":
        try {
          nativeBinding = require("./native-util.darwin-arm64.node");
        } catch (e) {
          loadError = e;
        }
        break;
      default:
        throw new Error(`Unsupported architecture on macOS: ${arch}`);
    }
    break;
  case "linux":
    switch (arch) {
      case "x64":
        if (isMusl()) {
          try {
            nativeBinding = require("./native-util.linux-x64-musl.node");
          } catch (e) {
            loadError = e;
          }
        } else {
          try {
            nativeBinding = require("./native-util.linux-x64-gnu.node");
          } catch (e) {
            loadError = e;
          }
        }
        break;
      case "arm64":
        if (isMusl()) {
          try {
            nativeBinding = require("./native-util.linux-arm64-musl.node");
          } catch (e) {
            loadError = e;
          }
        } else {
          try {
            nativeBinding = require("./native-util.linux-arm64-gnu.node");
          } catch (e) {
            loadError = e;
          }
        }
        break;
      case "arm":
        if (isMusl()) {
          try {
            nativeBinding = require("./native-util.linux-arm-musleabihf.node");
          } catch (e) {
            loadError = e;
          }
        } else {
          try {
            nativeBinding = require("./native-util.linux-arm-gnueabihf.node");
          } catch (e) {
            loadError = e;
          }
        }
        break;
      case "riscv64":
        if (isMusl()) {
          try {
            nativeBinding = require("./native-util.linux-riscv64-musl.node");
          } catch (e) {
            loadError = e;
          }
        } else {
          try {
            nativeBinding = require("./native-util.linux-riscv64-gnu.node");
          } catch (e) {
            loadError = e;
          }
        }
        break;
      case "s390x":
        try {
          nativeBinding = require("./native-util.linux-s390x-gnu.node");
        } catch (e) {
          loadError = e;
        }
        break;
      default:
        throw new Error(`Unsupported architecture on Linux: ${arch}`);
    }
    break;
  default:
    throw new Error(`Unsupported OS: ${platform}, architecture: ${arch}`);
}

if (!nativeBinding) {
  if (loadError) {
    throw loadError;
  }
  throw new Error(`Failed to load native binding`);
}

const { open } = nativeBinding;

module.exports.open = open;
